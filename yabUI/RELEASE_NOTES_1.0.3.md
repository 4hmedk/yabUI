# yabUI 1.0.3

Dragging a window tile now suppresses the tile's click action for the duration
of the drag. Dropping onto another tile performs only the requested swap or
split/reflow operation; it no longer focuses the window underneath the drop.
Normal clicks still focus a window in both the main workspace and menu-bar maps.
