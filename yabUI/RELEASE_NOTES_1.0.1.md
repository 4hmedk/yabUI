# yabUI 1.0.1

This patch release replaces the overview window map with one unified live map:

- visible windows from all spaces are shown together without display containers
- tiles are packed into a non-overlapping grid while preserving frame proportions
- space labels remain available without turning the map into nested cards
- clicking any app tile focuses its corresponding Yabai window
